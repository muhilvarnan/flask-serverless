version: 2.1

jobs:
  test:
    docker:
      - image: python:2.7-alpine3.8
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python -m unittest discover -s tests

workflows:
   version: 2.1
   alm-deploy:
     jobs:
       - test


